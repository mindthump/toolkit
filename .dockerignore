# We don't want the working directory's git and other stuff to COPY into the image.
# Add other files as needed.
.git/
.dockerignore
Dockerfile
.DS_Store
.idea/
.vagrant/
*.log
# We may want these in the future, but now it's kinda noise.
toolkit-*.yaml
